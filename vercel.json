{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)", // Обрабатываем API запросы
      "dest": "/app.js"
    },
    {
      "src": "/(.*)", // Все остальные маршруты
      "dest": "/app.js" // Убедитесь, что ваш app.js обрабатывает маршруты
    }
  ]
}
